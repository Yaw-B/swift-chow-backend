#!/bin/bash
cd "/c/Users/Owner/Downloads/SWIFT CHOW website project"
git add -A
git commit -m "Major: Fix logout persistence, remove footer My Account links, auto-load profile data"
git push origin master
