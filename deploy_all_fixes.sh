#!/bin/bash
cd "/c/Users/Owner/Downloads/SWIFT CHOW website project"
git add -A
git commit -m "Fix: OAuth callback handling, page protection, and login button visibility"
git pull origin master
git push origin master
